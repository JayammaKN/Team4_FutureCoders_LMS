# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tests\features\program\program-navigation.feature.spec.js >> Program Navigation >> Program page navigation
- Location: .features-gen\tests\features\program\program-navigation.feature.spec.js:10:3

# Error details

```
TypeError: Cannot read properties of undefined (reading 'step')
```

# Test source

```ts
  1  | //import { expect } from '@playwright/test';
  2  | import { createRequire } from 'module';
  3  | const require = createRequire(import.meta.url);
  4  | const loginData = require('../test-data/loginData.json');
  5  | const programData = require('../test-data/programData.json');
  6  | import logger from '../../utils/logger.js';
  7  | 
  8  | export class ProgramPage
  9  | 
  10 | {
  11 | constructor(page, envFixture)
  12 | {
  13 |     this.page=page;
  14 |     this.env = envFixture;
  15 | }
  16 | 
  17 | /*
  18 | async loginApplication()
  19 | {
  20 |   await this.page.goto('https://lms-frontend-hackathon-6dcccb9dd0fa.herokuapp.com/login');
  21 |   await this.page.locator("//input[@id='username']").fill('Lmshackathon@gmail.com');
  22 |   await this.page.locator("//input[@id='password']").fill('lmsAug@2026');
  23 |   await this.page.getByLabel('Select the role').getByText('Select the role').click();
  24 |   await this.page.getByRole('option', { name: 'Admin' }).click();
  25 |   await this.page.locator("//button[@id='login']").click();
  26 | }*/
  27 | 
  28 |   /*async loginApplication() {
  29 | 
  30 |     await this.page.goto(this.env.validUrl);
  31 |     await this.page.locator("//input[@id='username']").fill(this.env.username);
  32 |     await this.page.locator("//input[@id='password']").fill(this.env.password);
  33 |     await this.page.getByLabel('Select the role').getByText('Select the role').click();
  34 |     await this.page.getByRole('option', {name: this.env.role}).click();
  35 |     await this.page.locator("//button[@id='login']").click();
  36 |   }*/
  37 |    async loginApplication({ username = this.env.username, password = this.env.password, role = this.env.role } = {}) {
> 38 |      await this.test.step(`Login as ${username || this.env.username}`, async () => {
     |                      ^ TypeError: Cannot read properties of undefined (reading 'step')
  39 |        if (username) await this.page.fill(this.userField, username);
  40 |        if (password) await this.page.fill(this.passwordField, password);
  41 |        if (role) {
  42 |          await this.selectRole(role);
  43 |        }
  44 |        await this.page.click(this.loginButton);
  45 |        await this.waitForLoginResult();
  46 |        if (await this.isHomePage()) {
  47 |          logger.loginSuccess(username || this.env.username);
  48 |        } else {
  49 |          const errors = await this.getErrorMessages();
  50 |          logger.loginFailed(username || this.env.username, errors.join(' | ') || 'unknown');
  51 |        }
  52 |      });
  53 |    }
  54 | 
  55 |   async clickProgram() {
  56 |     await this.page.locator("//button[@id='program']").click();
  57 |   }
  58 | 
  59 | 
  60 | }
```